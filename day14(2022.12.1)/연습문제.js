  // 내가 푼거
        // var radius;
        // var value;
        // var width;

        // value = prompt("반지름의 넓이는 " , " ");
        // width = radius / value;
        // document.write(value);
        
        // 풀이
        let r;
         r = prompt("반지름의 값은?");
         // let r = prompt("반지름의 값은?");과 같다
        let cirArea = r * r * 3.14;

        document.write("원의 넓이는 " + cirArea);
