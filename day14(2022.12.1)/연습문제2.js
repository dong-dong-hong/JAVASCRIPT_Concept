let width;
let height;
let result;

width = prompt("가로 길이는?");
height = prompt("세로 길이는?");
result = width * height;
document.write("<h1> 삼각형의 넓이는? " + result + "</h1>" );
document.write("<h5> 가로 길이는? " + width + "</h5>" );
document.write("<h5> 세로 길이는? " + height+ "</h5>" );